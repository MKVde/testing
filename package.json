{
  "name": "freightcore-load-test",
  "version": "1.0.0",
  "description": "Contact form load testing for FreightCore with GUI Chrome",
  "main": "test-form.js",
  "scripts": {
    "test": "node test-form.js"
  },
  "dependencies": {
    "playwright": "^1.41.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
